import { SelectiondCardDirective } from './selectiond-card.directive';

describe('SelectiondCardDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectiondCardDirective();
    expect(directive).toBeTruthy();
  });
});
